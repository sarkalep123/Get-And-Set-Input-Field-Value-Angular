count=0

handleIncrement(){
this.count=this.count+1;
}
# Counter App Code

# app.ts

# different fun use

count=0

handleIncrement(){
this.count=this.count+1;
}

handleDecrement(){
this.count=this.count-1;;
}

handleReset(){
this.count=0;
}

# One function use

    handleCounter(val:string){
      if(val=='minus'){
        this.count=this.count-1
      }else if(val=='plus'){
        this.count=this.count+1
      }else{
        this.count=0
handleDecrement(){
this.count=this.count-1;;
}

handleReset(){
this.count=0;
}
